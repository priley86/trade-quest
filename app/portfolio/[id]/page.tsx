import { notFound } from "next/navigation";
import Link from "next/link";
import { requirePlayer } from "../../../lib/auth";
import { portfolio, readQuery } from "../../../lib/dolt";
import { AppShell } from "../../ui";
import { money, returnPercent } from "../../../lib/validation";
import { HistoryChart } from "../history-chart";
export default async function HoldingPage({params}:{params:Promise<{id:string}>}){const {profile}=await requirePlayer();const {id}=await params;const data=await portfolio(profile.public_player_id);const h=data?.holdings.find(x=>x.id===id);if(!data||!h)notFound();const history=await readQuery(`SELECT recorded_date,market_value_cents FROM holding_price_history WHERE holding_id='${id}' ORDER BY recorded_date DESC`);return <AppShell active="portfolio" profile={profile}><Link className="text-link" href="/">← Back to collection</Link><section className="pokemon-detail"><div className="detail-art">{h.image_url&&<img src={h.image_url} alt=""/>}</div><div><span className="eyebrow">Your collection</span><h1>{h.display_name}</h1><p className={`detail-price `}>{money(h.current_value_cents)}</p><p>Current value · <span className={h.current_value_cents >= h.cost_basis_cents ? "positive" : "negative"}>{returnPercent(h.current_value_cents,h.cost_basis_cents)}</span> since purchase</p></div><div className="price-history"><h2>Holding details</h2><dl className="snapshot-grid"><div><dt>Quantity</dt><dd>{h.quantity}</dd></div><div><dt>Purchase price</dt><dd>{money(h.cost_basis_cents)}</dd></div><div><dt>Current value</dt><dd>{money(h.current_value_cents)}</dd></div></dl>{h.product_url&&h.asset_type!=="sports_card"&&<a className="text-link" href={h.product_url} target="_blank" rel="noreferrer">Review card source ↗</a>}</div></section><section className="chart-card"><h2>Recorded value history</h2>{history.length > 1 && <HistoryChart values={[...history].reverse().map((p:any) => ({date: String(p.recorded_date), cents: Number(p.market_value_cents)}))} />}{history.length ? <ul>{history.map((point:any)=><li key={point.recorded_date}>{String(point.recorded_date).slice(0,10)} · {money(Number(point.market_value_cents))}</li>)}</ul> : <p>No price data recorded yet. Check back after the next daily update.</p>}</section></AppShell>}
