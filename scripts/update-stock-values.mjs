import mysql from "mysql2/promise";import {readFileSync} from "node:fs";try{for(const l of readFileSync('.env.local','utf8').split('\n')){const m=l.match(/^([^=]+)=(.*)$/);if(m&&!process.env[m[1]])process.env[m[1]]=m[2]}}catch{}
const db=await mysql.createConnection({uri:process.env.DOLT_DATABASE_URL||'mysql://root@127.0.0.1:3307/tradequest'}),h={'APCA-API-KEY-ID':process.env.ALPACA_API_KEY||'','APCA-API-SECRET-KEY':process.env.ALPACA_API_SECRET||''};const [rows]=await db.query("select id,asset_public_id,quantity from holdings where asset_type='stock'");console.log(`[stock-values] found ${rows.length} holding(s)`);for(const x of rows){try{const r=await fetch(`https://data.alpaca.markets/v2/stocks/${x.asset_public_id}/snapshot?feed=iex`,{headers:h});if(!r.ok)throw Error(`API HTTP ${r.status}`);const j=await r.json(),c=Math.round(Number(j.latestTrade?.p||j.dailyBar?.c||0)*100*Number(x.quantity));if(!c)throw Error('no price');await db.query('update holdings set current_value_cents=? where id=?',[c,x.id]);await db.query("insert into holding_price_history(holding_id,recorded_date,market_value_cents,source,card_api_id) values(?,current_date,?,'alpaca',?) on duplicate key update market_value_cents=values(market_value_cents)",[x.id,c,x.asset_public_id]);console.log(`[stock-values] UPDATED ${x.id} -> $${(c/100).toFixed(2)}`)}catch(e){console.error(`[stock-values] FAILED ${x.id}: ${e.message}`)}await new Promise(r=>setTimeout(r,350))}await db.end();
