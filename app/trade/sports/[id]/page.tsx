import {notFound} from "next/navigation"; import Link from "next/link"; import { BackButton } from "../../back-button"; import {requirePlayer} from "../../../../lib/auth"; import {getSportsProduct} from "../../../../lib/cardsight"; import {AppShell} from "../../../ui"; import {money} from "../../../../lib/validation"; import {SportsBuy} from "../detail";
export default async function SportsDetail({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{sport?:string}>}){const {profile}=await requirePlayer();const {id}=await params;const filters=await searchParams;const p=await getSportsProduct(id,filters?.sport||"");if(!p)notFound();return <AppShell active="trade" profile={profile}><BackButton label="← Back to sports cards" /><section className="pokemon-detail"><div className="detail-art"><img src={p.imageUrl} alt=""/></div><div><span className="eyebrow">CardSight market data</span><h1>{p.name}</h1><p className="detail-price">{money(p.marketPriceCents)}</p><p>Market price used in TradeQuest</p><SportsBuy product={p}/><dl className="snapshot-grid"><div><dt>Year</dt><dd>{p.year||"—"}</dd></div><div><dt>Manufacturer</dt><dd>{p.manufacturerName||"—"}</dd></div><div><dt>Release</dt><dd>{p.releaseName||"—"}</dd></div><div><dt>Set</dt><dd>{p.setName||"—"}</dd></div><div><dt>Card number</dt><dd>{p.cardNumber||"—"}</dd></div></dl></div><div className="price-history"><h2>Recent snapshot</h2><dl className="snapshot-grid"><div><dt>Market price</dt><dd>{money(p.marketPriceCents)}</dd></div><div><dt>Low sale price</dt><dd>{money(p.lowCents)}</dd></div><div><dt>Mid sale price</dt><dd>{money(p.midCents)}</dd></div><div><dt>High sale price</dt><dd>{money(p.highCents)}</dd></div></dl>{p.recentRecords?.length ? <><h2 className="recent-sales-heading">10 most recent sales</h2><table><thead><tr><th>Title</th><th>Price</th><th>Date</th><th>Source</th><th></th></tr></thead><tbody>{p.recentRecords.map((r,i)=><tr key={r.date + "-" + i}><td>{r.title}</td><td>{money(Math.round(r.price*100))}</td><td>{new Date(r.date).toLocaleDateString()}</td><td>{r.source}</td><td>{r.url&&<a className="text-link" href={r.url} target="_blank" rel="noreferrer">LINK</a>}</td></tr>)}</tbody></table></> : null}</div></section></AppShell>}