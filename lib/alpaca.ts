export type StockProduct={symbol:string;name:string;exchange:string;price:number;change:number;imageUrl?:string};
const base=process.env.ALPACA_DATA_URL||"https://data.alpaca.markets";
const headers={"APCA-API-KEY-ID":process.env.ALPACA_API_KEY||"","APCA-API-SECRET-KEY":process.env.ALPACA_API_SECRET||""};
export async function searchStocks(query:string):Promise<StockProduct[]>{if(!query.trim())return[];const r=await fetch(`${base}/v2/stocks/snapshots?symbols=${encodeURIComponent(query.toUpperCase())}&feed=iex`,{headers,cache:"no-store"});if(!r.ok)return[];const j=await r.json();return Object.entries(j).map(([symbol,v]:any)=>({symbol,name:symbol,exchange:"US",price:Number(v.latestTrade?.p||v.dailyBar?.c||0),change:Number(v.dailyBar?.c||0)-Number(v.prevDailyBar?.c||v.dailyBar?.c||0)}));}
export async function getStock(symbol:string){const r=await fetch(`${base}/v2/stocks/${encodeURIComponent(symbol)}/snapshot?feed=iex`,{headers,cache:"no-store"});if(!r.ok)return null;const v=await r.json();const end=new Date(),start=new Date(end);start.setFullYear(end.getFullYear()-1);const br=await fetch(`${base}/v2/stocks/${encodeURIComponent(symbol)}/bars?timeframe=1Day&start=${start.toISOString().slice(0,10)}&end=${end.toISOString().slice(0,10)}&limit=260&feed=iex&sort=asc`,{headers,cache:"no-store"});const bj=br.ok?await br.json():{};return {symbol,name:symbol,exchange:"US",price:Number(v.latestTrade?.p||v.dailyBar?.c||0),change:Number(v.dailyBar?.c||0)-Number(v.prevDailyBar?.c||v.dailyBar?.c||0),bars:(bj.bars||[]).map((b:any)=>({date:b.t,cents:Math.round(Number(b.c)*100)}))} as StockProduct&{bars:{date:string;cents:number}[]};}
