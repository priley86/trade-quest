import Link from "next/link"; import {requirePlayer} from "../../../lib/auth"; import {AppShell} from "../../ui"; import {searchStocks} from "../../../lib/alpaca";
export default async function StocksPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {profile}=await requirePlayer();const {q=""}=await searchParams;const results=await searchStocks(q);return <AppShell active="trade" profile={profile}><section className="page-intro"><span className="eyebrow">The Stock Trading Post</span><h1>Choose a stock</h1><p>Search for a company symbol and explore its latest market price.</p></section><form className="pokemon-search" method="get"><input name="q" defaultValue={q} placeholder="Search a stock symbol, like AAPL…"/><button className="primary-button">Search</button></form><p className="data-note">Alpaca market data</p>{q&&!results.length?<p className="form-status">No stock data found. Try a symbol such as AAPL.</p>:<section className="pokemon-grid">{results.map(s=><Link className="pokemon-card" key={s.symbol} href={`/trade/stocks/${s.symbol}`}><div className="category-icon stock large">📈</div><h2>{s.symbol}</h2><p>Latest market price</p><strong>${s.price.toFixed(2)}</strong></Link>)}</section>}</AppShell>}
