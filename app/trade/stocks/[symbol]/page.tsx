import {notFound} from "next/navigation"; import {requirePlayer} from "../../../../lib/auth"; import {getStock} from "../../../../lib/alpaca"; import {AppShell} from "../../../ui"; import {BackButton} from "../../back-button"; import {HistoryChart} from "../../../portfolio/history-chart"; import {StockBuy} from "../buy";
export default async function StockDetail({params}:{params:Promise<{symbol:string}>}){const {profile}=await requirePlayer();const {symbol}=await params;const stock=await getStock(symbol);if(!stock)notFound();const bars=stock.bars||[],chartBars=bars.filter((_,i,a)=>i===0||i===a.length-1||i%8===0),month=bars[Math.max(0,bars.length-22)]?.cents||0,year=bars[0]?.cents||0;const pct=(base:number)=>base?((stock.price*100-base)/base*100):0;return <AppShell active="trade" profile={profile}><BackButton label="← Back to stocks"/><section className="pokemon-detail"><div className="detail-art"><div className="category-icon stock large">📈</div></div><div><span className="eyebrow">Alpaca market data</span><h1>{stock.symbol}</h1><p>{stock.name}</p><p className="detail-price">${stock.price.toFixed(2)}</p><p>Latest market price</p><StockBuy symbol={stock.symbol} price={stock.price}/></div><div className="price-history"><h2>Recent snapshot</h2><dl className="snapshot-grid"><div><dt>Price</dt><dd>${stock.price.toFixed(2)}</dd></div><div><dt>Daily change</dt><dd className={stock.change>=0?"positive":"negative"}>{stock.change>=0?"+":""}${stock.change.toFixed(2)}</dd></div><div><dt>1-month change</dt><dd className={pct(month)>=0?"positive":"negative"}>{pct(month)>=0?"+":""}{pct(month).toFixed(1)}%</dd></div><div><dt>1-year change</dt><dd className={pct(year)>=0?"positive":"negative"}>{pct(year)>=0?"+":""}{pct(year).toFixed(1)}%</dd></div></dl></div></section>{chartBars.length>1&&<section className="chart-card stock-chart"><h2>1-year price history</h2><HistoryChart values={chartBars}/></section>}<p><a className="text-link" href={`https://finance.yahoo.com/quote/${stock.symbol}`} target="_blank" rel="noreferrer">Yahoo Finance ↗</a></p></AppShell>}
